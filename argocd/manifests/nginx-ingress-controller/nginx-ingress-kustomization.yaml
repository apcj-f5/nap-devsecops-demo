namespace: nginx-ingress
helmCharts:
  - name: nginx-ingress
    repo: https://helm.nginx.com/stable
    version: 0.18.0
    releaseName: nginx-ingress
    namespace: nginx-ingress
    includeCRDs: true
    # override with these custom values
    valuesFile: nginx-ingress-values.yaml
resources:
  - nginx-regcred-secret.yaml
  - tls-secret.yaml
